---
# Registry host is provisioned as infrastructure only.
# Registry software (mirror-registry + oc mirror v2) is configured by a separate project.
# Port and IP are defined here so security groups open the correct network paths.
registry_instance_type: t3.large
registry_root_volume_size: 50
registry_data_volume_size: 200
registry_port: 8443
