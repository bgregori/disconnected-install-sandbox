---
common_client_dns_server: "{{ services_private_ip }}"
common_client_ntp_server: "{{ services_private_ip }}"
common_client_search_domain: "{{ cluster_domain }}"
common_client_bastion_fallback_dns: "10.0.0.2"
